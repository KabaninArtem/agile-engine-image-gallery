import { PhotoViewDirective } from './photo-view.directive';

describe('PhotoViewDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoViewDirective();
    expect(directive).toBeTruthy();
  });
});
